# Repo-local defaults for the kite workflow.
schema_version: 1

scaffold:
  template: base
  license: MIT

workflow:
  test_tier: standard

audit:
  python:
    ignore_packages:
      - pip
      - setuptools
      - wheel

deploy:
  target: none
